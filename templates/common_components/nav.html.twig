<nav id="commonNavigationBar" class="navbar navbar-expand-lg bgCol ">
    <a id="findLabTitle" class="navbar-brand" href="{{ path("home") }}">FindLAB</a>

    <div class="navbar-collapse justify-content-end" id="navbarTogglerDemo03">
        {% if not is_granted('IS_AUTHENTICATED_REMEMBERED') %}
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link defaultNavLinks" href="{{ path("login") }}">Se connecter</a>
            </li>
            <li class="nav-item">
                <a class="nav-link defaultNavLinks" href="{{ path("candidate_register") }}">S'inscrire</a>
            </li>
        </ul>
        {% endif %}
    </div>
</nav>